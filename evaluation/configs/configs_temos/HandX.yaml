NAME: Bihand # Experiment name
DEBUG: False # Debug mode
ACCELERATOR: 'gpu' # Devices optioncal: “cpu”, “gpu”, “tpu”, “ipu”, “hpu”, “mps, “auto”
#DEVICE: [0,1,2,3] # Index of gpus eg. [0] or [0,1,2,3]
DEVICE: [0,1] # Index of gpus eg. [0] or [0,1,2,3]

# Training configuration
TRAIN:
  #---------------------------------
  STAGE: temos # stage "vae" or "diffusion", "vae_diffusion"
  #---------------------------------
  DATASETS: ['interact'] # Training datasets
  NUM_WORKERS: 8 # Number of workers 11
  BATCH_SIZE: 128 # Size of batches 128
  START_EPOCH: 0 # Start epochMMOTIONENCODER
  END_EPOCH: 1500 # End epoch
  RESUME: '' # Resume training from this path
  OPTIM:
    TYPE: AdamW # Optimizer type
    LR: 1e-4 # Learning rate

# Evaluating Configuration
EVAL:
  DATASETS: ['interact'] # Evaluating datasets
  BATCH_SIZE: 32 # Evaluating Batch size
  SPLIT: test

# Test Configuration
TEST:
  PRETRAINED_CHECKPOINTS_VAE: ''
  SAVE_PREDICTIONS: False
  CHECKPOINTS: '' # Pretrained model path
  DATASETS: ['interact'] # training datasets
  SPLIT: test
  BATCH_SIZE: 32 # training Batch size
  MEAN: False
  NUM_SAMPLES: 1
  FACT: 1
  # REPLICATION_TIM

# Datasets Configuration
DATASET:
  JOINT_TYPE: 'humanml3d' # join type
  VERSION: ''
  MOTION_TYPE: 'vector_263'
METRIC:
  # TYPE: ['TemosMetric', 'TM2TMetrics']
  TYPE: []
# Losses Configuration
LOSS:
  TYPE: temos # Losses type
  USE_INFONCE: True
  USE_INFONCE_FILTER: False
  LAMBDA_LATENT: 0.05 # Lambda for latent Losses
  LAMBDA_KL: 0.05 # Lambda for kl Losses # bigger 0.005
  LAMBDA_REC: 1 # Lambda for reconstruction Losses
  LAMBDA_GEN: 1.0 # Lambda for text-motion generation losses
  LAMBDA_CROSS: 1.0 # Lambda for reconstruction Losses
  LAMBDA_CYCLE: 0.0 # Lambda for cycle Losses
  LAMBDA_PRIOR: 0.0
  LAMBDA_INFONCE: 0.6 # Lambda for infonce
  INFONCE_TEMP: 0.05
  DIST_SYNC_ON_STEP: False # Sync Losses on step when distributed trained
  USE_RECLIPLOSS: False
  SYNC: False
  TRAIN_TMR: False

# Model Configuration
model:
  vae: true # whether vae model
  model_type: temos # model type
  condition: 'text'
  target: modules_temos
  #####
  latent_dim: 256 # latent dimension
  ff_size: 1024 #
  num_layers: 4 # number of layers
  num_head: 6 # number of head layers
  dropout: 0.1 # dropout rate
  activation: gelu # activation type
  eval_text_encode_way: given_glove
  eval_text_source: token

# Logger configuration
LOGGER:
  SAVE_CHECKPOINT_EPOCH: 10
  LOG_EVERY_STEPS: 1
  VAL_EVERY_STEPS: 10
  TENSORBOARD: True
  WANDB:
    PROJECT: null
    OFFLINE: False
    RESUME_ID: null
  SWANLAB:
    PROJECT: null
    EXPERIMENT_NAME: null